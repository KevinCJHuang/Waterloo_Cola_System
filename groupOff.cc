#include "groupOff.h"
#include "printer.h"



void Groupoff::main() {
	for (unsigned int i = 0; i < numStudents; i++) {
		_Accept (giftCard);
	}
}

WATCard::FWATCard Groupoff::giftCard() {
	WATCard::FWATCard

}

